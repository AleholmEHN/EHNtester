

function start()
{
    line(0,totalHeight,totalWidth, totalHeight, 10, "black");
    
    line(0, totalHeight/2, totalWidth, totalHeight/2, 5, "red");
    
    line(totalWidth/2, 0, totalWidth/2, totalHeight, 4, "yellow");
    
}